<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Hello Discuss</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <nav class="light-blue">
      <div class="nav-wrapper container">
        <div class="row">
          <a href="<%= Routes.topic_path(@conn, :index) %>" class="brand-logo">
            Discussions
          </a>
          <ul class="right">
          <%= if @conn.assigns[:user] do %>
            <%= link "Sign out", to:  Routes.auth_path(@conn, :request, "github") %>
          <% else %>
            <%= link "Sign in", to:  Routes.auth_path(@conn, :request, "github") %>
          <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="row">
      <div class="alert alert-info col s12" role="alert"><%= get_flash(@conn, :info) %></div>
      <div class="alert alert-danger col s12" role="alert"><%= get_flash(@conn, :error) %></div>
    </div>
    <%= @inner_content %>
  </body>
</html>
